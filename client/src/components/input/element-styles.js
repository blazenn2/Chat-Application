export const elementStyles = (additionalClasses) => {
  const classes = `w-full bg-text-box focus:outline-none rounded-md p-3 placeholder:text-sm text-text-primary text-monstserrat ${additionalClasses}`;
  return classes;
};
